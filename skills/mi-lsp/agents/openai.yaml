interface:
  display_name: "mi-lsp"
  short_description: "Semantic CLI for code and .docs/wiki canon"
  default_prompt: "Use $mi-lsp to explore this local codebase and its .docs/wiki canon with mi-lsp. If the mi-lsp binary is missing, install/bootstrap it first instead of falling back immediately. Then avoid grep-heavy loops and summarize the strongest semantic evidence without requiring MCP."
