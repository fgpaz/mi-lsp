interface:
  display_name: "mi-lsp"
  short_description: "Agent-first semantic CLI without MCP"
  default_prompt: "Use $mi-lsp to explore this local codebase with mi-lsp, avoid grep-heavy loops, and summarize the strongest semantic evidence without requiring MCP."

