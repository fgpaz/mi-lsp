# T0 - Plan, Session Contract, Decision Lock

Create the persistent plan, task packets, and `.docs/auditoria/2026-05-27-mi-lsp-edit-plan-v2-go-ast/session-contract.yaml`.

Verify:

- governance is unblocked
- selected mode is `orquestado_deterministico`
- branch/worktree match the session contract
- release gate is required
- forbidden paths include `.git/**`, `.mi-lsp/**`, secrets, env files, binaries, and `.docs/wiki/_mi-lsp/read-model.toml`

