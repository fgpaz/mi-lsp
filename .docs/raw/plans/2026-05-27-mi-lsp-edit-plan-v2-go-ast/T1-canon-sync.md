# T1 - Canon Sync

Update RF/TP/TECH/CT docs for `edit-plan-v2`.

Required canon updates:

- `RF-QRY-018`: extend requirement from v1 textual edit plans to v2 structural Go AST backend.
- `CT-NAV-EDIT-PLAN`: document v1/v2 version switch, v2 language metadata, Go AST operations, and `language_not_supported`.
- `TP-QRY` and `06_matriz_pruebas_RF.md`: add AST and language compatibility tests.
- `07_baseline_tecnica.md` and `09_contratos_tecnicos.md`: record that only Go AST is implemented in this wave.

