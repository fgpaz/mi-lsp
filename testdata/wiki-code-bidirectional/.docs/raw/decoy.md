# Decoy raw

Este archivo es un señuelo en `.docs/raw`. Contiene menciones de `runDemo` y `RF-DEMO-001` pero no es evidencia canonica.

El codigo de candidate/ranking no debe promocionar estos resultados como primarios.