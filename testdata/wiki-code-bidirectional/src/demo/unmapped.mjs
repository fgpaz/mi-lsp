// src/demo/unmapped.mjs
// No wiki binding — should be classified as unmapped_changed_code.

/**
 * @description Standalone module with no associated wiki document.
 * @returns {string}
 */
export function standaloneFeature() {
  return 'unmapped';
}