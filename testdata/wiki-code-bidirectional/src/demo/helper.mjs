// src/demo/helper.mjs
// Supporting code called by service.mjs

/**
 * @description Helper computation (supporting_code, not direct implementation).
 * @param {number} value
 * @returns {number}
 */
export function compute(value) {
  return value * 2;
}